Read Me

Part 3 Response
Our implementation has starvation because we only fill requests the same way each time
and do not monitor which philosopher has already eaten.

To run each problem. Go to the top level directory and type

make qN

where N is the question number 2-5 to run the solution for. 